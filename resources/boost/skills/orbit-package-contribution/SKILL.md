---
name: orbit-package-contribution
description: Contribute to cms-orbit/* Composer packages safely. Activate when opening PRs, tagging releases, or editing code under packages/cms-orbit without leaking host-project files or breaking standalone Laravel installs.
---

# Contributing to cms-orbit packages

## When to use

- Committing to `cms-orbit/core`, `announcement`, `popup`, `saas`, or `blog`
- Preparing a release (version in `composer.json`, CHANGELOG, git tag)
- Reviewing whether a change requires host manual setup

## Independence checklist

1. **Scope:** Only change files inside the target package (and its tests). Never commit host
   `app/`, `routes/web.php`, or demo entities unless they are published stubs from Core.
2. **Composer:** Register providers via `extra.laravel.providers`; avoid `dont-discover: true`
   unless there is a documented reason.
3. **Routes & migrations:** Load from the package (`loadMigrationsFrom`, `Route::middleware()->group()`).
4. **Frontend:** Ship pages inside the package; declare them in `resources/orbit/frontend.json`.
   Host bridges are generated by `php artisan orbit:frontend-sync`, not copied manually into the package.
5. **Config:** Use `mergeConfigFrom` + optional `vendor:publish --tag=...-config` for overrides.
6. **README:** Document required vs optional host steps clearly.
7. **Boost:** Add/update `resources/boost/guidelines/*.md` and `resources/boost/skills/*/SKILL.md`.
8. **i18n:** Every user-visible string through `__()` / `useT()` with `ko.json` updates.
9. **Release:** Bump `"version"` in package `composer.json`, update CHANGELOG, tag, push.
   Align dependent package constraints (e.g. `"cms-orbit/core": "4.0.0"`).

## Verify standalone install

In a clean Laravel app (or CI):

```bash
composer require cms-orbit/{package}
php artisan orbit:install --no-interaction   # core only
php artisan migrate
php artisan orbit:frontend-sync
php artisan test --compact
```

## Never include from the host project

- `.env`, API keys, customer data
- Host-specific entities, policies, or Vite entrypoints
- Paths like `/Users/...` or monorepo-only aliases without `frontend.json` + sync command
